{
  "name": "svelte-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && touch dist/.nojekyll && cp dist/index.html dist/404.html"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.1.0",
    "svelte": "^5.37.3",
    "vite": "^7.1.0",
    "vite-plugin-singlefile": "^2.3.0"
  }
}
